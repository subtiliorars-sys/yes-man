# Yes Man — Office Hours

**Autonomous worker:** hourly weekdays 9:30–5:30 (see `agent-corps/fleet/repo-registry.json`).

**Pickup:** read `WAVES.md` — first pending wave, one PR per wave. Branch: `automation/wave-*`.

**Verify:** `npm run verify` (tsc + vitest + vite build).

**Ethics:** no dark patterns — see `GOVERNANCE.md` and `MeniscusMaximus/YES_MAN_GDD.md`.

**Never merge your own PR.** Owner merges same day when green.
